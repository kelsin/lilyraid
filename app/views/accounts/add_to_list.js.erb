// Remove Add to List item
$('#add_to_loot_list dd.account_<%= @account %>').fadeOut(1000, function () {
    $(this).remove();
});

// Add new loot list item
$('#loot_list').html('<%= escape_javascript(render(:partial => "raids/position", :locals => { :position => @lp, :raid => @raid })) %>');

