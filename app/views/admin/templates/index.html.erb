<div class="raid">
  <% if @templates.size > 0 %>
    <h3>Templates</h3>
    <dl>
      <%= render @templates %>
    </dl>
  <% end %>
  <h3>New Template</h3>
  <% form_for [:admin, @new_template] do |f| %>
    <%= error_messages_for(:new_template) %>
    <dl>
      <dt><%= f.label :name %></dt>
      <dd><%= f.text_field :name %></dd>
      <dt><%= f.label :number_of_slots %></dt>
      <dd>
        <%= f.text_field :number_of_slots, :size => 3 %>
      </dd>
      <dt></dt>
      <dd><%= submit_tag 'Create' %></dd>
    </dl>
  <% end %>
</div>
